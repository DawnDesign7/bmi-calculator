function bodymass (weight, height) {
return alert
}
var height= Number(prompt ("Enter Height in metres"));
var weight= Number(prompt("Enter Weight in Kgs"));
var finalHeight= height*height;
var bmi = weight / finalHeight;

alert("Your BMI is" + bmi.toFixed(2) );
